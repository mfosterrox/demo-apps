apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: postgres-secrets
  namespace: backend
  labels:
    app: postgres
rules:
- apiGroups: [""]       # core
  resources: ["secrets"]
  verbs: ["get"]

