apiVersion: apps/v1
kind: Deployment
metadata:
  name: wordpress
  namespace: frontend
  labels:
    app: wordpress
    demo: roadshow
    repo: demo-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wordpress
  template:
    metadata:
      labels:
        app: wordpress
        demo: roadshow
        repo: demo-apps
    spec:
      securityContext:
        fsGroup: 1001
        supplementalGroups: [1001]
      initContainers:
      - name: volume-permissions
        image: bitnami/wordpress:latest
        command: ['sh', '-c']
        args:
          - |
            mkdir -p /bitnami/wordpress
            chown -R 1001:1001 /bitnami/wordpress
            chmod -R 775 /bitnami/wordpress
            # Ensure the directory is writable for the WordPress user
            find /bitnami/wordpress -type d -exec chmod 775 {} \;
            find /bitnami/wordpress -type f -exec chmod 664 {} \;
        securityContext:
          runAsUser: 0
        volumeMounts:
        - name: wordpress-data
          mountPath: /bitnami/wordpress
      containers:
      - image: bitnami/wordpress:latest
        name: wordpress
        securityContext:
          runAsUser: 1001
          runAsGroup: 1001
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        ports:
        - containerPort: 80
          protocol: TCP
        env:
        - name: WORDPRESS_DATABASE_HOST
          value: "wordpress.frontend.svc.cluster.local" 
        - name: WORDPRESS_DATABASE_USER
          value: "wordpress"
        - name: WORDPRESS_DATABASE_PASSWORD
          value: "wordpress"
        - name: WORDPRESS_DATABASE_NAME
          value: "wordpress"
        volumeMounts:
        - name: wordpress-data
          mountPath: /bitnami/wordpress 
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 60
          periodSeconds: 20
      volumes:
      - name: wordpress-data
        persistentVolumeClaim:
          claimName: wordpress-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-pvc
  namespace: frontend
  labels:
    demo: roadshow
    repo: demo-apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ocs-external-storagecluster-ceph-rbd
  resources:
    requests:
      storage: 10Gi
